#include <cstring>
#include <iostream>
#include <fstream>
#include <vector>

using namespace std;

int main(int argc, char* argv[]){

    fstream fin(argv[1]), fout;
    

    // int N, p1, p2;
    // fstream fin(argv[1]), fout;

    // fin >> N;
    // vector<int> chords(N, 0), result;
    // while (fin >> p1 >> p2){
    //     chords[p1] = p2; chords[p2] = p1;
    // }
    // fin.close();

    // fout.open(argv[2],ios::out);
    // fout << M[N-1][0] << endl;
    // for(int i=result.size()-1; i>=0; i--){
    //     fout << result[i] << " " << chords[result[i]] << endl;
    // }
    // fout.close();

    return 0;
}